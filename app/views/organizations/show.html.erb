<% if flash[:errors] %>
  <% flash[:errors].each do |error| %>
    <p style="color:red;"><%= error %></p>
  <% end %>
<% end %>

<% if @user %>
  <a href="/logout">Logout</a> || <a href="/organizations">Organizations</a>
<% end %>

<h1>Organization Name: <%= @organization.name %> <button>Join Group</button></h1> 
<h3>Description: <%= @organization.description %> </h3>

<h3> Created By: <%= @user.first_name %> <%= @user.last_name %></h3>
<h3> Members: </h3>
<% @organization.members.each do |member| %>
  <%= organization.member.first_name %>
  <br>
<% end %>